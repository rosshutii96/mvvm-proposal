export interface DeviceModel {
  name: string;
  deviceType: string;
  make: string;
  model: string;
  platform: string;
  appVersion: string;
  lastUsedOn: string;
  lastLocation: string;
  registeredOn: string;
  registeredBy: string;
  blocked: boolean;
  deviceId: string;
}
